# Сутності домену - Довідник дорожніх знаків

## 📊 Огляд сутностей

| Сутність | Піддомен | Опис |
|----------|----------|-------|
| RoadSign | Каталог знаків | Основний об'єкт системи - дорожній знак |
| SignCategory | Каталог знаків | Категорія для групування знаків |
| User | Користувачі та доступ | Користувач системи |
| Admin | Управління контентом | Адміністратор з правами управління |

## RoadSign (Дорожній знак)

### Атрибути:
- **id** (Integer) - унікальний ідентифікатор
- **name** (String) - назва знака (наприклад: "Стоп", "Головна дорога")
- **category** (String) - категорія знака
- **description** (Text) - детальний опис знака та його призначення
- **image_url** (String) - посилання на зображення знака
- **created_at** (DateTime) - дата додавання знака
- **updated_at** (DateTime) - дата останнього оновлення

### Методи:
- `update_description(new_description)` - оновити опис знака
- `change_category(new_category)` - змінити категорію
- `get_details()` - отримати детальну інформацію про знак

### Інваріанти:
- Назва знака не може бути порожньою
- Категорія повинна бути з попередньо визначеного списку

## 📂 SignCategory (Категорія знака)

### Атрибути:
- **name** (String) - назва категорії
- **description** (String) - опис категорії
- **signs** (List[RoadSign]) - список знаків у категорії

### Методи:
- `add_sign(sign)` - додати знак до категорії
- `remove_sign(sign)` - видалити знак з категорії
- `get_signs_count()` - отримати кількість знаків
- `list_signs()` - отримати список всіх знаків категорії

### Доменні події:
- `SignAddedToCategory` - знак додано до категорії
- `SignRemovedFromCategory` - знак видалено з категорії

## 👤 User (Користувач)

### Атрибути:
- **id** (Integer) - унікальний ідентифікатор
- **username** (String) - ім'я користувача
- **email** (String) - електронна пошта
- **role** (String) - роль (guest, admin)
- **created_at** (DateTime) - дата реєстрації

### Методи:
- `is_admin()` - перевірити, чи є адміністратором
- `promote_to_admin()` - надати права адміністратора
- `demote_to_guest()` - забрати права адміністратора

## 🔧 Admin (Адміністратор)

### Атрибути:
- Наслідує всі атрибути User
- **permissions** (List[String]) - список дозволів

### Методи:
- `can_manage_content()` - перевірити право управління контентом
- `add_sign(sign_data)` - додати новий дорожній знак
- `edit_sign(sign_id, new_data)` - редагувати існуючий знак
- `delete_sign(sign_id)` - видалити дорожній знак

### Доменні події:
- `SignCreated` - створено новий знак
- `SignUpdated` - оновлено існуючий знак
- `SignDeleted` - видалено знак

## 🔗 Зв'язки між сутностями
